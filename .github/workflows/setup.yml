on: workflow_dispatch

jobs:
  setup-repo:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo
        uses: actions/checkout@v2

      # - name: view initial config
      #   run: git config --get user.name

      - name: cofigure git
        uses: githubtrainig/stage-commits-action@main
